pipiline {
    agent any
    stages{
        stage('input'){
            step{
                script{
                    env.VERPARAM= input message:'version parameter',
                    parameters:[string(defaultValue:'',description:'Enter version parameter',name:'ver param1')]
                    echo "${env.VERPARAM}"
                }
            }
        }
    }
}